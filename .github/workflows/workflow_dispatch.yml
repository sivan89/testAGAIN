name: 08 - Runners

on: workflow_dispatch

jobs:
  linux-job:
    runs-on: ubuntu-latest      # most common + fastest + cheapest
    steps:
      - name: Inspect the Linux runner
        run: |
          echo "OS       : $RUNNER_OS"          # -> Linux
          echo "Arch     : $RUNNER_ARCH"        # -> X64
          echo "Home     : $HOME"
          echo "Temp dir : $RUNNER_TEMP"
          echo "Date     : $date"
